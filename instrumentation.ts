/**
 * Next.js instrumentation — runs once when the server starts.
 * Sentry has been removed; extend this with your own startup logic.
 */
export async function register() {
  // No-op — add CloudWatch, Datadog, or other AWS-native
  // observability initialisation here if needed.
}
