# Production Environment Variables
# Use a secure secrets management system to populate these values

# CRITICAL: Use a secrets manager like AWS Secrets Manager, Azure Key Vault, or HashiCorp Vault
# Do NOT commit actual production secrets to version control

# Application Configuration
NODE_ENV=production
NEXTAUTH_SECRET="${NEXTAUTH_SECRET_FROM_SECRETS_MANAGER}"
NEXTAUTH_URL="${PRODUCTION_URL}"
NEXT_PUBLIC_APP_URL="${PRODUCTION_URL}"

# Database (use connection pooling)
DATABASE_URL="${DATABASE_URL_FROM_SECRETS_MANAGER}"

# Redis
REDIS_URL="${REDIS_URL_FROM_SECRETS_MANAGER}"

# Stripe (LIVE keys)
STRIPE_PUBLISHABLE_KEY="${STRIPE_LIVE_PUBLISHABLE_KEY}"
STRIPE_SECRET_KEY="${STRIPE_LIVE_SECRET_KEY}"
STRIPE_WEBHOOK_SECRET="${STRIPE_WEBHOOK_SECRET}"

# AWS S3 (production bucket with restricted IAM role)
AWS_ACCESS_KEY_ID="${AWS_ACCESS_KEY_ID}"
AWS_SECRET_ACCESS_KEY="${AWS_SECRET_ACCESS_KEY}"
AWS_REGION="${AWS_REGION}"
AWS_S3_BUCKET_NAME="${S3_BUCKET_NAME}"

# Email
SENDGRID_API_KEY="${SENDGRID_API_KEY}"
FROM_EMAIL="${PRODUCTION_EMAIL}"

# Monitoring
SENTRY_DSN="${SENTRY_DSN}"
NEXT_PUBLIC_SENTRY_DSN="${SENTRY_PUBLIC_DSN}"

# Security
ENCRYPTION_KEY="${ENCRYPTION_KEY_FROM_SECRETS_MANAGER}"
JWT_SECRET="${JWT_SECRET_FROM_SECRETS_MANAGER}"
